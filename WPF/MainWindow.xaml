<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="Demo Redis 3-layer" Height="220" Width="420">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="User Id:" Margin="10" VerticalAlignment="Center"/>
        <TextBox x:Name="UserIdTextBox" Grid.Column="1" Margin="10" Width="200"/>
        <Button x:Name="FetchButton" Grid.Column="2" Margin="10" Width="120" Height="32" Content="Get Profile" Click="FetchButton_Click"/>

        <TextBlock Grid.Row="1" Grid.ColumnSpan="3" Margin="10,0,10,10" Text="Kết quả:"/>
        <TextBox x:Name="ResultTextBox" Grid.Row="2" Grid.ColumnSpan="3" Margin="10" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
